<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"

    android:background="@color/fondo_arena">

    <!-- EditText para la nueva contraseña -->
    <EditText
        android:id="@+id/inputNuevaContraseña"
        android:layout_width="0dp"
        android:layout_height="76dp"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/background_input_text"
        android:hint="Nueva Contraseña"
        android:inputType="textPassword"
        android:textColor="#ffeccc"
        android:textColorHint="#dfe0df"
        android:textSize="28sp"
        android:gravity="center"
        android:fontFamily="casual"
        app:layout_constraintBottom_toTopOf="@+id/inputConfirmarContraseña"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- EditText para la confirmación de la contraseña -->
    <EditText
        android:id="@+id/inputConfirmarContraseña"
        android:layout_width="0dp"
        android:layout_height="76dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/background_input_text"
        android:hint="Confirmar Contraseña"
        android:inputType="textPassword"
        android:textColor="#ffeccc"
        android:textColorHint="#dfe0df"
        android:textSize="28sp"
        android:gravity="center"
        android:fontFamily="casual"
        app:layout_constraintBottom_toTopOf="@+id/btAceptar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/inputNuevaContraseña" />

    <!-- Botón de aceptar para cambiar la contraseña -->

    <!-- Botón de cancelar para cerrar el diálogo -->

    <Button
        android:id="@+id/btAceptar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/background_button_login"
        android:fontFamily="casual"
        android:padding="10dp"
        android:text="Cambiar Contraseña"
        android:textColor="#ffeccc"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/inputConfirmarContraseña" />

    <Button
        android:id="@+id/btCancelar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/background_button_login"
        android:fontFamily="casual"
        android:text="Cancelar"
        android:textColor="#ffeccc"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/inputConfirmarContraseña"
        app:layout_constraintVertical_bias="1.0" />

</androidx.constraintlayout.widget.ConstraintLayout>
