<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondo_arena">

    <!-- Rectangle background -->
    <FrameLayout
        android:id="@+id/frameMenu"
        android:layout_width="276dp"
        android:layout_height="394dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"

        android:layout_marginStart="67dp"
        android:layout_marginTop="168dp"
        android:layout_marginEnd="67dp"
        android:layout_marginBottom="168dp"
        android:background="@drawable/fondo_fragment">

        <!-- ImageView (centered inside the FrameLayout) -->
        <ImageView
            android:id="@+id/imagenMenu"
            android:layout_width="200dp"
            android:layout_height="500dp"
            android:layout_gravity="center"
            android:src="@drawable/j_2048" />

        <!-- Title TextView (aligned at the bottom center) -->
        <TextView
            android:id="@+id/tituloMenu"
            android:textSize="32dp"
            android:padding="25dp"
            android:fontFamily="casual"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|center_horizontal"
            android:text="My Fragment"

            android:textColor="@android:color/black"
            />

        <!-- Record TextView (aligned at the top center) -->
        <TextView
            android:id="@+id/recordMenu"
            android:fontFamily="casual"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:padding="25dp"
            android:text="My Fragment"
            android:textColor="@android:color/black"
            android:textSize="32dp" />

    </FrameLayout>

</RelativeLayout>
